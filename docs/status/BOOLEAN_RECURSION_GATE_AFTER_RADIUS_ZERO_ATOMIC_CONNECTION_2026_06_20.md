# Boolean recursion gate after radius-zero atomic connection

Date: 2026-06-20
After HEAD: `ea50536`

## Status

`BOOLEAN_RECURSION_GATE_TOUCHED_STATUS_TEST_ONLY`

## Prerequisite now available

`RADIUS_ZERO_ATOMIC_PRESERVATION_CONNECTION_PROVED`

The radius-zero atomic connection is locked, so the Boolean-recursion gate can
now be touched as a bounded status test.

## Available atomic radius

```text
0
Available atomic theorems
equality_atom_locality_input_radius_zero
relation_atom_locality_input_radius_zero
Classification
GATE_OPEN_FOR_BOUNDED_BOOLEAN_STATUS_TEST_ONLY
Weakest missing object
A proved Boolean constructor layer preserving a shared radius across formula
constructors.
Boundary
This gate does not claim:
Boolean recursion theorem proved;
quantifier recursion theorem proved;
arbitrary formula locality;
formula radius construction;
positive-radius atomic preservation;
full Gaifman locality.
The next admissible object is a bounded Boolean radius-zero constructor target.
